fix: critical UX fixes for real user onboarding

P0 fixes:
- Add scripts/__init__.py (pip install devsquad command now works)
- Add pyyaml>=6.0 to pyproject.toml dependencies

P1 fixes:
- --stream flag now actually passes through to Worker layer
- Unify env var: DEVSQUAD_LLM_BACKEND (config_loader supports both names)
- Add English keywords to all 7 roles in ROLE_REGISTRY
- Add prominent MOCK MODE banner at top of mock reports
- Add --lang en/zh/ja/auto option with i18n support in to_markdown()
- Replace legacy trae_agent.py reference in INSTALL.md Windows section

P2 fixes:
- Dockerfile: remove 2>/dev/null error suppression
- cli.py: add Python 3.9+ version check
- README.md: fix DEVSQUAD_BACKEND -> DEVSQUAD_LLM_BACKEND

99/99 tests passing
